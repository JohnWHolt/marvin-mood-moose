import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Marvin the Bipolar Mood Moose',
  description: 'An emotionally intelligent AI companion for exploring bipolar experiences',
  keywords: 'bipolar, mental health, AI companion, emotional intelligence, therapy',
  authors: [{ name: 'Marvin Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
